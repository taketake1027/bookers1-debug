<h1>Editing Book</h1>

<%= form_with(model: @book, local: true) do |form| %>
  <%= render 'form', book: @book %>  <!-- _form.html.erbを呼び出し -->
  <!-- ボタンをここに追加しない -->
<% end %>

<%= link_to 'Show', @book %> |
<%= link_to 'Back', books_path %>
